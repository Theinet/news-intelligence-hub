{
  "name": "news-intelligence-hub",
  "private": true,
  "workspaces": [
    "apps/server",
    "apps/web"
  ],
  "scripts": {
    "build": "npm run build --workspace apps/server && npm run build --workspace apps/web",
    "dev": "concurrently \"npm run start:dev --workspace apps/server\" \"npm run dev --workspace apps/web\"",
    "lint": "npm run lint --workspace apps/server && npm run lint --workspace apps/web",
    "test": "npm run test --workspace apps/server",
    "check:unicode": "node scripts/check-unicode.mjs",
    "screenshots": "node scripts/capture-screenshots.mjs"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "playwright": "^1.60.0"
  }
}
